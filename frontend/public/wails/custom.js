// Stub served by Vite so the Wails runtime's optional-script probe
// (/wails/custom.js) gets a clean 200 in browser preview instead of a 404.
// The desktop app injects its own runtime file at this path; this stub is
// never used there.
