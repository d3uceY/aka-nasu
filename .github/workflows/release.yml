name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  validate-tag:
    runs-on: ubuntu-latest
    outputs:
      is_stable: ${{ steps.check.outputs.is_stable }}
      version: ${{ steps.check.outputs.version }}
    steps:
      - name: Validate stable release tag
        id: check
        run: |
          TAG="${GITHUB_REF_NAME}"
          if [[ "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "is_stable=true" >> "$GITHUB_OUTPUT"
            echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"
          else
            echo "is_stable=false" >> "$GITHUB_OUTPUT"
            echo "version=" >> "$GITHUB_OUTPUT"
          fi

  build-linux-amd64:
    runs-on: ubuntu-24.04
    needs: validate-tag
    if: needs.validate-tag.outputs.is_stable == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install system deps
        run: |
          sudo apt update
          sudo apt install -y build-essential pkg-config libgtk-4-dev libwebkitgtk-6.0-dev libayatana-appindicator3-dev

      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v3/cmd/wails3@latest

      - name: go mod tidy
        run: go mod tidy

      - name: Install frontend dependencies
        run: npm install
        working-directory: frontend

      - name: Generate bindings
        run: wails3 generate bindings -f '-tags production' -clean=true -ts

      - name: Build frontend
        run: npm run build
        working-directory: frontend

      - name: Inject version into nfpm config
        run: |
          VERSION="${{ needs.validate-tag.outputs.version }}"
          sed -i "s/^version: .*/version: \"${VERSION}\"/" build/linux/nfpm/nfpm.yaml

      - name: Generate .desktop file
        run: wails3 generate .desktop -name "aka-nasu" -exec "aka-nasu" -icon "aka-nasu" -outputfile "${{ github.workspace }}/build/linux/aka-nasu.desktop" -categories "Development;"

      - name: Build Linux binary
        env:
          CGO_ENABLED: "1"
          GOOS: linux
          GOARCH: amd64
        run: go build -tags production -trimpath -buildvcs=false -ldflags="-w -s -X main.Version=${{ needs.validate-tag.outputs.version }}" -o bin/aka-nasu

      - name: Build .deb package
        env:
          GOARCH: amd64
        run: wails3 tool package -name "aka-nasu" -format deb -config ./build/linux/nfpm/nfpm.yaml -out bin

      - name: Collect Linux artifacts
        run: |
          mkdir -p build/bin
          cp bin/aka-nasu build/bin/aka-nasu-linux-amd64
          cp bin/*.deb build/bin/aka-nasu-linux-amd64.deb

      - uses: actions/upload-artifact@v4
        with:
          name: binaries-linux-amd64
          path: |
            build/bin/aka-nasu-linux-amd64
            build/bin/aka-nasu-linux-amd64.deb

  build-macos:
    runs-on: macos-latest
    needs: validate-tag
    if: needs.validate-tag.outputs.is_stable == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v3/cmd/wails3@latest

      - name: go mod tidy
        run: go mod tidy

      - name: Install frontend dependencies
        run: npm install
        working-directory: frontend

      - name: Generate bindings
        run: wails3 generate bindings -f '-tags production' -clean=true -ts

      - name: Build frontend
        run: npm run build
        working-directory: frontend

      - name: Inject version into Info.plist
        run: |
          VERSION="${{ needs.validate-tag.outputs.version }}"
          sed -i '' \
            -e "s/{{.Info.ProductVersion}}/${VERSION}/g" \
            -e "s/{{.Info.ProductName}}/aka-nasu/g" \
            -e "s/{{.OutputFilename}}/aka-nasu/g" \
            -e "s/{{.Name}}/aka-nasu/g" \
            -e "s/{{.Info.Comments}}/Built using Wails (https:\/\/wails.io)/g" \
            -e "s/{{.Info.Copyright}}/Copyright © 2026 d3uceY/g" \
            build/darwin/Info.plist

      - name: Build macOS ARM64 binary + DMG
        env:
          CGO_ENABLED: "1"
          GOOS: darwin
          GOARCH: arm64
          CGO_CFLAGS: "-mmacosx-version-min=12.0"
          CGO_LDFLAGS: "-mmacosx-version-min=12.0"
          MACOSX_DEPLOYMENT_TARGET: "12.0"
        run: |
          mkdir -p artifacts
          go build -tags production -trimpath -buildvcs=false -ldflags="-w -s -X main.Version=${{ needs.validate-tag.outputs.version }}" -o build/bin/aka-nasu
          APP=build/bin/aka-nasu-arm64.app/Contents
          mkdir -p "$APP/MacOS" "$APP/Resources"
          cp build/darwin/icons.icns "$APP/Resources/iconfile.icns"
          cp build/bin/aka-nasu       "$APP/MacOS/aka-nasu"
          cp build/darwin/Info.plist "$APP/Info.plist"
          hdiutil create -volname "aka-nasu" -srcfolder build/bin/aka-nasu-arm64.app \
            -ov -format UDZO artifacts/aka-nasu-macos-arm64.dmg

      - name: Build macOS AMD64 binary + DMG
        env:
          CGO_ENABLED: "1"
          GOOS: darwin
          GOARCH: amd64
          CGO_CFLAGS: "-mmacosx-version-min=12.0"
          CGO_LDFLAGS: "-mmacosx-version-min=12.0"
          MACOSX_DEPLOYMENT_TARGET: "12.0"
        run: |
          go build -tags production -trimpath -buildvcs=false -ldflags="-w -s -X main.Version=${{ needs.validate-tag.outputs.version }}" -o build/bin/aka-nasu
          APP=build/bin/aka-nasu-amd64.app/Contents
          mkdir -p "$APP/MacOS" "$APP/Resources"
          cp build/darwin/icons.icns "$APP/Resources/iconfile.icns"
          cp build/bin/aka-nasu       "$APP/MacOS/aka-nasu"
          cp build/darwin/Info.plist "$APP/Info.plist"
          hdiutil create -volname "aka-nasu" -srcfolder build/bin/aka-nasu-amd64.app \
            -ov -format UDZO artifacts/aka-nasu-macos-amd64.dmg

      - uses: actions/upload-artifact@v4
        with:
          name: binaries-macos
          path: artifacts/*.dmg

  build-windows-amd64:
    runs-on: windows-latest
    needs: validate-tag
    if: needs.validate-tag.outputs.is_stable == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install NSIS
        run: choco install nsis -y

      - name: Add NSIS to PATH
        shell: pwsh
        run: |
          $candidates = @(
            "$env:ChocolateyInstall\bin",
            "${env:ProgramFiles(x86)}\NSIS",
            "$env:ProgramFiles\NSIS"
          )
          foreach ($candidate in $candidates) {
            if (Test-Path $candidate) {
              Add-Content -Path $env:GITHUB_PATH -Value $candidate
              $env:PATH = "$candidate;$env:PATH"
            }
          }
          $makensis = Get-Command makensis -ErrorAction SilentlyContinue
          if (-not $makensis) {
            throw "makensis not found after NSIS installation"
          }
          Write-Host "Using makensis at $($makensis.Source)"

      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v3/cmd/wails3@latest

      - name: go mod tidy
        run: go mod tidy

      - name: Install frontend dependencies
        run: npm install
        working-directory: frontend

      - name: Generate bindings
        shell: bash
        run: wails3 generate bindings -f '-tags production' -clean=true -ts

      - name: Build frontend
        run: npm run build
        working-directory: frontend

      - name: Inject version into Windows metadata
        shell: bash
        run: |
          VERSION="${{ needs.validate-tag.outputs.version }}"
          sed -i "s/\"ProductVersion\": \"[^\"]*\"/\"ProductVersion\": \"${VERSION}\"/" build/windows/info.json
          sed -i "s/\"file_version\": \"[^\"]*\"/\"file_version\": \"${VERSION}\"/" build/windows/info.json
          sed -i "/assemblyIdentity/s/version=\"[^\"]*\"/version=\"${VERSION}\"/" build/windows/wails.exe.manifest
          sed -i "s/!define INFO_PRODUCTVERSION \"[^\"]*\"/!define INFO_PRODUCTVERSION \"${VERSION}\"/" build/windows/nsis/wails_tools.nsh

      - name: Generate .syso resource
        working-directory: build
        shell: bash
        run: wails3 generate syso -arch amd64 -icon windows/icon.ico -manifest windows/wails.exe.manifest -info windows/info.json -out ../wails_windows_amd64.syso

      - name: Build Windows binary
        env:
          CGO_ENABLED: "0"
          GOOS: windows
          GOARCH: amd64
        shell: bash
        run: |
          mkdir -p build/bin
          go build -tags production -trimpath -buildvcs=false -ldflags="-w -s -H windowsgui -X main.Version=${{ needs.validate-tag.outputs.version }}" -o build/bin/aka-nasu-windows-amd64.exe
          rm -f wails_windows_amd64.syso

      - name: Generate WebView2 bootstrapper
        shell: bash
        run: wails3 generate webview2bootstrapper -dir "build/windows/nsis"

      - name: Build NSIS installer
        shell: cmd
        run: makensis -DARG_WAILS_AMD64_BINARY=..\..\bin\aka-nasu-windows-amd64.exe build\windows\nsis\project.nsi

      - name: Collect artifacts
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist
          Copy-Item build\bin\aka-nasu-windows-amd64.exe dist\aka-nasu-windows-amd64.exe
          $installer = Get-ChildItem build\bin\*installer* -ErrorAction SilentlyContinue | Select-Object -First 1
          if (-not $installer) {
            throw "Windows installer not found in build/bin"
          }
          Copy-Item $installer.FullName dist\aka-nasu-windows-amd64-installer.exe

      - uses: actions/upload-artifact@v4
        with:
          name: binaries-windows-amd64
          path: dist/*

  cleanup-tag-on-failure:
    runs-on: ubuntu-latest
    needs:
      - build-linux-amd64
      - build-macos
      - build-windows-amd64
    if: |
      always() && (
        needs.build-linux-amd64.result == 'failure' ||
        needs.build-macos.result == 'failure' ||
        needs.build-windows-amd64.result == 'failure'
      )
    permissions:
      contents: write
    steps:
      - name: Delete tag
        run: |
          git -c "http.extraHeader=Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            push origin --delete "${{ github.ref_name }}" || true

  release:
    runs-on: ubuntu-latest
    needs:
      - validate-tag
      - build-linux-amd64
      - build-macos
      - build-windows-amd64
    if: needs.validate-tag.outputs.is_stable == 'true'
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate release body
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CURR_TAG="${GITHUB_REF#refs/tags/}"
          PREV_TAG=$(git tag --sort=-version:refname | grep -E '^v' | sed -n '2p')

          if [ -n "$PREV_TAG" ]; then
            RANGE="${PREV_TAG}..${CURR_TAG}"
          else
            RANGE="${CURR_TAG}"
          fi

          # ── Download table ─────────────────────────────────────────────
          {
            echo '## Download'
            echo ''
            echo '| Platform | Link | Note |'
            echo '|---|---|---|'
            echo "| Windows (installer) | [aka-nasu-windows-amd64-installer.exe](https://github.com/${{ github.repository }}/releases/download/${CURR_TAG}/aka-nasu-windows-amd64-installer.exe) | ⚠️ SmartScreen will block it - see First-Run Notes below |"
            echo "| Windows (portable)  | [aka-nasu-windows-amd64.exe](https://github.com/${{ github.repository }}/releases/download/${CURR_TAG}/aka-nasu-windows-amd64.exe) | |"
            echo "| macOS Apple Silicon | [aka-nasu-macos-arm64.dmg](https://github.com/${{ github.repository }}/releases/download/${CURR_TAG}/aka-nasu-macos-arm64.dmg) | ⚠️ Gatekeeper will block it - see First-Run Notes below |"
            echo "| macOS Intel         | [aka-nasu-macos-amd64.dmg](https://github.com/${{ github.repository }}/releases/download/${CURR_TAG}/aka-nasu-macos-amd64.dmg) | ⚠️ Gatekeeper will block it - see First-Run Notes below |"
            echo "| Linux x86-64        | [aka-nasu-linux-amd64](https://github.com/${{ github.repository }}/releases/download/${CURR_TAG}/aka-nasu-linux-amd64) | |"
            echo "| Linux (.deb)        | [aka-nasu-linux-amd64.deb](https://github.com/${{ github.repository }}/releases/download/${CURR_TAG}/aka-nasu-linux-amd64.deb) | |"
            echo ''
            echo 'SHA-256 checksums are in \`SHA256SUMS.txt\` attached below.'
            echo ''
            echo '## First-Run Notes'
            echo ''
            echo 'aka-nasu is unsigned, so first launch may warn you - it'"'"'s safe and open source.'
            echo ''
            echo '- **Windows:** SmartScreen -> **More info** -> **Run anyway** (or unblock the `.exe` in Properties).'
            echo '- **macOS:** Right-click the `.dmg` -> **Open** -> **Open** again (or System Settings -> Privacy & Security -> Open Anyway).'
            echo '- **Linux:** `chmod +x aka-nasu-linux-amd64 && ./aka-nasu-linux-amd64`.'
            echo ''
          } > commit_log.md

          # ── Changelog ──────────────────────────────────────────────────
          COMMITS_FILE=$(mktemp)
          USERS_FILE=$(mktemp)
          PR_FILE=$(mktemp)
          PR_FOUND=false
          REPO="${{ github.repository }}"

          while IFS=$'\t' read -r HASH MSG; do
            [ -z "$HASH" ] && continue
            COMMIT_JSON=$(curl -sf \
              -H "Authorization: Bearer ${GH_TOKEN}" \
              -H "Accept: application/vnd.github+json" \
              "https://api.github.com/repos/${REPO}/commits/${HASH}" 2>/dev/null || echo "{}")
            GH_LOGIN=$(echo "$COMMIT_JSON" | jq -r '.author.login // empty' 2>/dev/null || echo "")
            PR_JSON=$(curl -sf \
              -H "Authorization: Bearer ${GH_TOKEN}" \
              -H "Accept: application/vnd.github+json" \
              "https://api.github.com/repos/${REPO}/commits/${HASH}/pulls" 2>/dev/null || echo "[]")
            PR_LINE=$(echo "$PR_JSON" | jq -r \
              --arg repo "$REPO" \
              --arg author "${GH_LOGIN}" \
              '.[0] | select(. != null) | "- \(.title) [#\(.number)](https://github.com/\($repo)/pull/\(.number))" + (if $author != "" then " - @\($author)" else "" end)' \
              2>/dev/null || echo "")
            if [ -n "$PR_LINE" ]; then
              echo "$PR_LINE" >> "$PR_FILE"
              PR_FOUND=true
            fi
            if [ -n "$GH_LOGIN" ]; then
              echo "$GH_LOGIN" >> "$USERS_FILE"
              echo "- ${MSG} - @${GH_LOGIN}" >> "$COMMITS_FILE"
            else
              echo "- ${MSG}" >> "$COMMITS_FILE"
            fi
          done < <(git log "$RANGE" --pretty=format:"%H%x09%s" --no-merges)

          # Always show both PRs and commits
          echo "## What's Changed" >> commit_log.md
          echo "" >> commit_log.md

          if [ "$PR_FOUND" = true ] && [ -s "$PR_FILE" ]; then
            echo "### Pull Requests" >> commit_log.md
            echo "" >> commit_log.md
            sort -u "$PR_FILE" >> commit_log.md
            echo "" >> commit_log.md
          fi

          if [ -s "$COMMITS_FILE" ]; then
            echo "### Commits" >> commit_log.md
            echo "" >> commit_log.md
            cat "$COMMITS_FILE" >> commit_log.md
            echo "" >> commit_log.md
          fi

          if [ -s "$USERS_FILE" ]; then
            echo "### Contributors" >> commit_log.md
            echo "" >> commit_log.md
            echo "<table><tr>" >> commit_log.md
            for USER in $(sort -u "$USERS_FILE"); do
              printf '  <td align="center"><a href="https://github.com/%s"><img src="https://github.com/%s.png?size=72" width="72" height="72" style="border-radius:50%%" /><br /><b>@%s</b></a></td>\n' \
                "$USER" "$USER" "$USER" >> commit_log.md
            done
            echo "</tr></table>" >> commit_log.md
            echo "" >> commit_log.md
          fi

          rm -f "$COMMITS_FILE" "$USERS_FILE" "$PR_FILE"

      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Verify release artifacts
        run: |
          test -f artifacts/aka-nasu-linux-amd64
          test -f artifacts/aka-nasu-linux-amd64.deb
          test -f artifacts/aka-nasu-macos-arm64.dmg
          test -f artifacts/aka-nasu-macos-amd64.dmg
          test -f artifacts/aka-nasu-windows-amd64-installer.exe
          test -f artifacts/aka-nasu-windows-amd64.exe

      - name: Generate checksums
        run: |
          cd artifacts
          sha256sum aka-nasu-* > SHA256SUMS.txt

      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          append_body: true
          body_path: commit_log.md
          files: artifacts/*
