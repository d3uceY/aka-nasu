/* Ambient module declarations for the docs landing page (editor-only; the
   Docusaurus build transpiles without tsc, so these keep the TS language
   server quiet for non-TS assets). */

declare module '*.css';

declare module '@fontsource-variable/fraunces/opsz.css';
declare module '@fontsource-variable/fraunces/opsz-italic.css';
declare module '@fontsource-variable/instrument-sans';
